---
veracode_scanner_enabled: yes
veracode_scanner_api_username: "{{ mandatory }}"
veracode_scanner_api_password: "{{ mandatory }}"
veracode_scanner_team: "{{ mandatory }}"
veracode_scanner_business_unit: "{{ mandatory }}"
veracode_scanner_business_criticality: "{{ mandatory }}"
veracode_scanner_repo: "{{ mandatory }}"
veracode_scanner_app_name: "{{ mandatory }}"

veracode_slack_upload: yes
veracode_scanner_slack_token: ""
veracode_scanner_slack_channel: ""

veracode_scanner_cron_shedule: "15 15 * * MON,THU"
veracode_scanner_cron_user: root
